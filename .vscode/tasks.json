{
	"version": "2.0.0",
	"tasks": [
	  {
		"label": "Build with Make",
		"type": "shell",
		"command": "make",
		"args": ["re"],
		"group": {
		  "kind": "build",
		  "isDefault": true
		},
		"problemMatcher": [],
		"detail": "Ejecuta 'make re' para compilar el proyecto.",
		"options": {
		  "cwd": "${workspaceFolder}/ex01" // Cambia a la ruta donde está tu Makefile
		}
	  }
	]
  }